name: GitHub Actions Demo
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: |
            REPO="${{ secrets.JIRA_BASE_URL }}"
            DB_NAME="${REPO//\./_}"
            echo $DB_NAME
